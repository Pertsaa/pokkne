{
  "name": "pokkne",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "prepare": "svelte-kit sync",
  "scripts": {
    "start": "pnpm --stream -r start",
    "start:dev": "pnpm --parallel --stream -r run start:dev",
    "db:generate": "pnpm --parallel --stream -r run db:generate",
    "db:push": "pnpm --parallel --stream -r run db:push",
    "build": "pnpm --stream -r run build",
    "clean": "rm -rf .parcel-cache && pnpm -r run clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
